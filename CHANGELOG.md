# Changelog

## [2.1.0] - 2026-06-13 — 조과 기록 · 검색 · 통계 · 워터마크 · 카메라 UX

### 화면 이동
- **다른 날짜 메모에서 오늘로 바로 가기**: 과거 날짜 메모를 연 상태에서 상단 "오늘" 버튼으로 오늘 메모 화면에 즉시 이동

### 경로 기록
- **활성화 즉시 현재 위치 1포인트 기록 + 카운트 표시**: 정지 상태에서도 버튼을 켜는 즉시 한 점이 저장되고 카운트가 바로 갱신
- **기록 포맷 통일**: 트랙 포인트를 메모와 동일한 `날짜시각 | 🛰 GPS` 순서로 기록 (구 포맷 하위호환 유지)
- 라벨 정리: "이동 경로 기록" → "경로 기록"

### 조과(어종/길이) 기록
- **어종·길이 필드 추가**: 메모 엔트리에 어종(`fishSpecies`)·길이(`fishLength`) 구조화 저장. `.md` 포맷 하위호환
- **텍스트 자동 인식**: 음성/수동 메모 내용에서 어종("감성돔")·길이("38cm/38센치/38짜리")를 자동 추출해 입력
- **어종 목록 사용자 관리**: 설정에서 어종 추가/삭제, 메모 작성 시 직접 입력한 신규 어종 자동 등록, 가나다순 정렬, 중복 체크·실시간 필터링·순번 표시
- **마릿수 단순화**: "사진/메모 1건 = 1마리" 기준으로 집계 (마릿수 입력 제거)

### 검색
- 메모 목록에 **검색** 진입점 추가. 텍스트·어종·장소(주소) 검색 + 어종 칩·날짜 범위 필터
- 전체 엔트리 1회 로드 후 메모리 필터링(즉시 반응), 날짜별 그룹 결과 → 탭 시 해당 메모로 이동

### 조과 통계
- **통계 대시보드**: 기간(전체/올해/이번 달) + 요약(총 마릿수·어종 수·기록일) + 어종별 집계(최대 길이 PB) + 월별 막대

### 워터마크
- **자유 위치(플로팅)**: 4코너 고정 → 임의 위치. 설정 미리보기·일반 카메라·AR 카메라 모두 **드래그로 이동**
  (잡은 지점 기준 상대 이동으로 통일, 점프·지연 없음)
- **날짜 포맷 추가**: `연-월-일 시:분:초`, `연-월-일 시:분`. 결합 포맷 선택 시 시간 라인 자동 토글
- **아이콘 통일**: 설정·일반 카메라·AR 카메라 모두 물방울(`water_drop`) 아이콘, 카메라 토글은 우측 중앙 배치

### 카메라 UX
- **세로 고정 + 컨트롤 회전**: 가로로 돌려도 레이아웃(촬영 버튼 등)이 고정되고, 컨트롤·워터마크 박스만 90° 회전(`sensors_plus` 기반). AR 카메라도 동일(`OrientationEventListener`)
- **워터마크 위치 즉시 반영**: 카메라/AR에서 옮긴 위치가 설정에 저장되고 촬영 사진에 동일 적용

### 품질
- 단위 테스트 확충(직렬화·통계·어종/길이 탐지·설정 영속화 등) — 총 **150개 통과**
- 기존 stale 테스트 정리, `_countEntries` 카운트 버그 수정, 트래킹 토글 화면 깜빡임 수정
- 정적 분석 경고 전면 정리(`flutter analyze` 0건): 미사용 코드/import 제거, 수온 NaN 비교 버그 수정, `RadioGroup` 신규 API 마이그레이션

## [1.2.6] - 2026-06-11

### 버그 수정
- 구글 드라이브 동기화 설정 시 계정 선택 후 앱이 오류 없이 종료되는 문제 수정

## [1.2.5] - 2026-06-11

### 버그 수정
- 앱 재시작 후 빠른 메모 실행 모드가 설정값과 무관하게 볼륨 버튼으로 초기화되는 문제 수정

## [1.2.4] - 2026-06-11

### 기능 추가
- **구글 드라이브 자동 백업**
  - Wi-Fi 연결 시 메모 저장/삭제마다 드라이브에 자동 동기화
  - 동기화 범위 선택: 메모 파일만 / 이미지·동영상 포함
  - 지금 전체 백업: 기존 파일 일괄 업로드
  - 지금 복원하기: 드라이브 데이터를 로컬과 날짜 기준 병합
  - 설정 → 백업 및 동기화 메뉴에서 관리

## [1.2.3] - 2026-06-11

### 기능 추가
- **날씨 자동 기록**
  - 메모 저장 시 풍속, 풍향, 날씨 상태 자동 수집 (Open-Meteo API, 무료·무제한)
  - 위치 현황 카드에 바람/날씨 정보 표시
  - 메모 파일에 날씨 정보 자동 기록 (`🌬 남서 3.2m/s | ⛅ 맑음` 형식)

## [1.2.2] - 2026-06-10

### 버그 수정
- 설정 화면 권한 카운트가 실제와 다르게 표시되는 문제 수정
  - Android에서 권한 요청 시 내부적으로 추가 반환되는 항목을 필터링

### 기능 개선
- **음성 메모**
  - 음성 입력 후 자동 저장 기본값 활성화로 변경
  - 빠른 메모 실행 기본값 볼륨 버튼 → 흔들기로 변경
  - 메모 버튼 탭 시 마이크 자동 활성화 제거 (시트 내 마이크 버튼 탭 시에만 활성화)
- **설정 화면**
  - 메뉴 설명 텍스트 폰트 크기(13sp) 및 색상 통일
  - 개발자 후원하기 기능 추가 (카카오페이 연동)

## [1.2.1] - 2026-06-10

### 버그 수정
- AR 카메라 첫 번째 사진이 메모에 표시되지 않는 문제 수정
  - Android MediaStore의 첫 번째 파일 경로가 빈 문자열로 반환되는 케이스 처리

### 기능 개선
- **지도**
  - 지도 진입 시 메모 및 트래킹 위치 전체가 한 화면에 보이도록 카메라 자동 조절
  - 위치 정보가 없을 때 현재 위치로 이동
  - 현재 위치로 돌아가는 버튼 추가
  - 시간 표시 버튼 활성화 시 트래킹 포인트에도 시간 표시
- **알림**
  - 위치 트래킹 서비스 알림 문구 간소화 (불필요한 단축키 안내 제거)
- **설정**
  - 저장 위치 변경 메뉴 설명 문구 개선 (메모 및 사진 저장 경로 모두 포함됨을 명시)
- **조석/수온 API**
  - 수온 조회를 KHOA API에서 Open-Meteo Marine API(무료·무제한)로 전환
  - 내륙·강가 낚시터에서도 가장 가까운 해안 관측소 수온 표시
  - 조석 예보 일별 캐싱 추가 (동일 관측소·날짜 중복 요청 방지)

## [1.2.0] - 2026-06-04

- 이동경로 트래킹 기능 개선 및 지도 UI 개편
